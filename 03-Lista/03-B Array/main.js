const names = ["Felipe", "João", "Julia", 10, false];

const joao = names[1];

// Adciona PEDRO
names.push("Pedro");

console.log(names);

//ADCIONA no INICIO DA STRING
names.unshift("20Anos-Fernanda");

console.log(names);

// Criando um array vazio
let meuArray = [];

// Adicionando elementos ao array
meuArray.push("elemento 1");
meuArray.push("elemento 2");
meuArray.push("elemento 3");

// Imprimindo o array

console.log(meuArray); 

// ["elemento 1", "elemento 2", "elemento 3"]
// Alguns métodos úteis para trabalhar com arrays em JavaScript incluem push(), pop(), shift(), unshift(), slice(), splice(), concat(), join(), reverse(), sort(), indexOf(), lastIndexOf(), forEach(), map(), filter(), reduce(), e every() 1